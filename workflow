dependencies:

    - json server
        -install, db.json, run
    - axios
        -install
        -instance
            api:create baseUrl
            urls
    - state management 
        - redux
        - react-redux
    - react-router-dom
